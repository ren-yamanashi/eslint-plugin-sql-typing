{
  "name": "@eslint-plugin-sql-typing/example-mysql2",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:setup": "docker compose exec mysql mysql -uroot -ptest test_db < schema.sql"
  },
  "dependencies": {
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@typescript-eslint/parser": "^8.55.0",
    "eslint": "^9.0.0",
    "eslint-plugin-sql-typing": "workspace:*",
    "typescript": "^5.0.0"
  }
}
